def tower_of_hanoi(n, a, b, c):
    """
    >>> tower_of_hanoi(1,'A','B','C')
    Move disk 1 from column A to column C
    """
    assert n >= 1
    if n == 1:
        return print("Move disk 1 from column", a, "to column", c)


    #tower_of_hanoi(n-1, a, b, c)
    #print("Move disk", n, "from column", a, "to column", c)
    #tower_of_hanoi(n-1, b, c, a)


